version: '2'
services:
  web:
    image: clevo/nlp-flask
    volumes:
      - ./app:/app
    ports:
      - "80:80"
    environment:
      - FLASK_APP=main.py
      - FLASK_DEBUG=1
      - 'RUN=flask run --host=0.0.0.0 --port=80'
    command: bash -c "cd /app && flask run --host=0.0.0.0 --port=80"
